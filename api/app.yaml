runtime: python312
instance_class: F2
entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT
handlers:
  - url: /.*
    script: auto
    secure: always
    login: optional
automatic_scaling:
  min_idle_instances: 1
  max_instances: 10
  target_cpu_utilization: 0.6
